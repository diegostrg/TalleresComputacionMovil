<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.taller11">

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RandomGreetActivity" android:exported="true" />
        <activity android:name=".ActivityTicTacToe" android:exported="true" />
        <activity android:name=".PaisesActivity" android:exported="true" />
        <activity android:name=".DetallePaisActivity" android:exported="true" />
    </application>

</manifest>